sudo: required
services:
    - docker

before_install:
  - docker build -t ashishdhawal/docker-reactapp -f Dcokerfile.dev .

script:
  - docker run ashishdhawal/docker-reactapp npm run test -- --coverage
   

